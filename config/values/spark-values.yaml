# Spark operator configuration — keep runtime credentials in SparkApplication secrets
# or Vault/External Secrets, not in this committed values file.

spark:
  jobNamespaces:
    - data-stack

webhook:
  enable: true

controller:
  replicas: 1

sparkJobNamespace: data-stack

serviceAccounts:
  spark:
    create: true
    name: spark
